/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // If you have any experimental features, they should be compatible with App Router
}

module.exports = nextConfig